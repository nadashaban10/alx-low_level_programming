#include <stdio.h> 
/**
 * main: entry-point
 * description: print all the alphabet litter 
 * return 0 always success
*/

int main(void)

	char ch = 'a';
	while(ch < = 'z')
{
	putchar(ch);
}
	ch = ch + 1
	putchar(\n);
	return (0);
}
