#include <stdio.h>

/**
 *main - Entry point
 *
 *description: print all alphabet letters
 *
 *return: always 0 (success)
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar('\n');
		ch++;
	}
		return (0);
	}
