#include "main.h"


/**
*_isupper  - checks the upper case letter.
*@c: The number to check letter
*Return:(1) if uppercase (0) for else
*/


int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
	else
	return (0);
}
